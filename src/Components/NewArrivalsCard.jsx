import { StyleSheet, Text, View, Image } from "react-native";
import React from 'react';

const NewArrivalsCard = ({name, price, description, img}) => {
    return(
        <View className="max-w[150] justify-center overflow-hidden mr-6">
            <Image source={{  }}/>
        </View>
    )
}